# 模型库层 (Model Library Layer)

**本轮迭代不实现**——预留目录占位。

## 规划（后续版本）

- 模型注册表：管理多个检测/预测模型的元数据（名称、版本、参数量、精度）
- 模型版本切换：运行时动态选择不同模型（如 TSPulse vs MOMENT 对比）
- 定时/触发计算：周期性健康评估、退化趋势计算

当前模型实例化由 `services/` 层直接调用 `algorithm/` 中的具体类完成，无需经过模型库。引入模型库后，服务层改为从注册表按名称查找。
